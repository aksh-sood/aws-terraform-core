.terraform
.terraform.tfstate.lock.info
terraform.tfstate
terraform.tfstate.d
terraform.tfstate.backup
*.tfstate
*.tfplan
.backup
.claude

# Real variable values. The *.tfvars.example files are committed; the filled-in
# copies hold account ids, subnet ids and webhook secrets.
*.tfvars
*.auto.tfvars.json
pipeline
.github